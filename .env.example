# Base de datos
DATABASE_URL=postgresql://user:password@localhost:5432/observatorio_discapacidad

# PubMed (opcional — 10 req/s con key, 3 sin key)
PUBMED_API_KEY=

# Redis (opcional — caché)
REDIS_URL=redis://localhost:6379

# Puerto
PORT=8000
